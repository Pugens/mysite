{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello",
            "problemMatcher": []
        },
        {
            "label": "Install package",
            "type": "shell",
            "command": "conda activate django-tutorial && pip list --format=freeze > requirements.txt"
        }
    ]
}